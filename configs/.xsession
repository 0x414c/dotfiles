#!/bin/sh

##
## ~/.xsession
##


## This script is called from Display Manager at login
## and evaluated by POSIX shell.


## `.xinitrc' can be evaluated from here:
# source "${HOME}/"'.xinitrc'
# exec /bin/bash/ --login "${HOME}/"'.xinitrc'
# /bin/bash --login -i "${HOME}/"'.xinitrc'

## EOF
